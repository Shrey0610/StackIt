const express = require('express');
const router = express.Router();

// Placeholder routes for answers
router.get('/', (req, res) => {
    res.json({ message: 'Answers API - Coming soon' });
});

module.exports = router;
